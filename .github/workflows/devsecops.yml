# Goal - Run the end-to-end DevSecOps CI pipeline
name: DevSecOps

on:
  push:
    branches:
      - code_quality

jobs:
  # Code Stage with Shift Left
  code-quality:
    uses: ./.github/workflows/code-quality.yml